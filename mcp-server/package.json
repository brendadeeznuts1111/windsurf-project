{
  "name": "bun-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for searching Bun documentation",
  "type": "module",
  "main": "bun-mcp-server.ts",
  "scripts": {
    "start": "bun run bun-mcp-server.ts",
    "dev": "bun --watch bun-mcp-server.ts",
    "build": "bun build bun-mcp-server.ts --outdir ./dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
